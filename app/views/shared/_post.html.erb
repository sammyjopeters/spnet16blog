<div class="card" data-id="<%= post.id %>">
  <div class="card__container card__container--closed">
    <svg class="card__image" xmlns="http://www.w3.org/2000/svg"
         xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 1920 1200"
         preserveAspectRatio="xMidYMid slice">
      <defs>
        <clipPath id="<%= post.id %>-clip-path">
          <polygon class="clip" points="0,1200 0,0 1920,0 1920,1200"></polygon>
        </clipPath>
      </defs>
      <image clip-path="url(#<%= post.id %>-clip-path)#disqus_thread" width="1920" height="1200"
             xlink:href="<%= image_url("posts/#{post.main_image}") %>"></image>
    </svg>
    <div class="card__content">
      <i class="card__btn-close fa fa-times"></i>
      <div class="card__caption">
        <h2 class="card__title"><%= post.title %></h2>
        <p class="card__subtitle"><%= post.byline %></p>
      </div>
      <div class="card__copy">
        <div class="meta">
          <img class="meta__avatar" src="<%= image_url('sjo.png') %>"/>
          <span class="meta__author"><%= post.author %></span>
          <span class="meta__date"><%= post.created_at %></span>
        </div>
        <%= post.body %>
      </div>
    </div>
  </div>
</div>
